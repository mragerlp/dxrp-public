# lpbitcoin - duplicate report

**Law:** duplicate filenames across packages are NOT merge signals - separate gameplay packages.

## Within-package filename collisions

- None (unique primary mesh names per slot, or single occurrence).

## Cross-package filename matches (same leaf name - do NOT merge packages)

- No cross-package primary mesh filename collisions involving this package.

